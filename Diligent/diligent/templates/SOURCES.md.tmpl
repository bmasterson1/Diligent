# Sources

<!-- This file tracks all source documents registered with diligent.
     Each source is an H2 heading with the source ID, followed by a
     fenced YAML code block with metadata.

     Format:

     ## DEAL-001
     ```yaml
     path: "documents/cim.pdf"
     date_received: "2026-01-15"
     parties:
       - "Seller LLC"
       - "Broker Inc"
     workstream_tags:
       - financial
       - legal
     supersedes: null
     notes: "Confidential Information Memorandum"
     ```

     Source IDs follow the pattern {DEAL_CODE}-{NNN} (zero-padded).
     IDs are monotonically increasing and never reused.

     This file is managed by diligent commands. Manual edits are
     supported but must preserve the H2 + fenced YAML structure. -->

---
deal_code: ${DEAL_CODE}
target_legal_name: "${TARGET_LEGAL_NAME}"
target_common_name: "${TARGET_COMMON_NAME}"
deal_stage: "${DEAL_STAGE}"
loi_date: "${LOI_DATE}"
principal: "${PRINCIPAL}"
principal_role: "${PRINCIPAL_ROLE}"
seller: "${SELLER}"
broker: "${BROKER}"
workstreams:
${WORKSTREAMS_YAML}
---
<!-- Investment thesis. Written by the analyst at init time.
     This is freeform markdown prose -- paragraphs, bullets, whatever
     communicates the deal rationale. Maintained manually. -->

${THESIS}

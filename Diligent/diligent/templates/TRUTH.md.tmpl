# Truth

<!-- This file contains validated facts about the target business.
     Each fact is an H2 heading with a fenced YAML code block containing
     the current value, source citation, date, workstream, and history.

     Format:

     ## fact_key_name
     ```yaml
     value: "the validated value (always quoted)"
     source: DEAL-001
     date: "2026-01-01"
     workstream: financial
     supersedes: []
     ```

     Facts are stored in flat alphabetical order by key.
     The workstream field must match an entry in WORKSTREAMS.md.
     The source field must reference an ID in SOURCES.md.

     This file is managed by diligent commands. Manual edits are
     supported but must preserve the H2 + fenced YAML structure. -->

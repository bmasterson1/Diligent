# Workstreams

<!-- This file lists the active workstreams for this deal.
     Each workstream is an H2 heading followed by a fenced YAML
     code block with name and status.

     Format:

     ## financial
     ```yaml
     name: financial
     status: active
     ```

     Workstreams are selected at init time and can be added later
     with `diligent config`. The workstream name is used as a tag
     in TRUTH.md and SOURCES.md for filtering.

     This file is managed by diligent commands. Manual edits are
     supported but must preserve the H2 + fenced YAML structure. -->

${WORKSTREAM_ENTRIES}

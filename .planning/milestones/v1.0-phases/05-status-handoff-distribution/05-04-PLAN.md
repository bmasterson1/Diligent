---
phase: 05-status-handoff-distribution
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - Diligent/pyproject.toml
  - Diligent/README.md
autonomous: false
requirements: [DIST-01]

must_haves:
  truths:
    - "Package builds successfully with hatch build"
    - "Built wheel includes diligent/skills/dd_*.md files"
    - "pyproject.toml has correct package name (resolved from PyPI availability)"
    - "README.md is under 100 lines with description, install command, quickstart, and 'what it is not' section"
    - "README renders correctly as PyPI long_description"
    - "CLI entry point is 'diligent' regardless of package name"
  artifacts:
    - path: "Diligent/pyproject.toml"
      provides: "Updated package configuration for PyPI"
      contains: "readme"
    - path: "Diligent/README.md"
      provides: "PyPI readme with quickstart"
      min_lines: 30
  key_links:
    - from: "Diligent/pyproject.toml"
      to: "Diligent/README.md"
      via: "readme field in project metadata"
      pattern: 'readme\s*='
    - from: "Diligent/pyproject.toml"
      to: "Diligent/diligent/skills/"
      via: "hatchling build includes package data"
      pattern: "packages.*diligent"
---

<objective>
Resolve PyPI package name, update pyproject.toml for distribution, write README, and verify the build includes all necessary files.

Purpose: This is the final plan that prepares diligent for PyPI distribution. The package name must be available on PyPI, the README must be concise and useful, and the built wheel must include skill template files.

Output: Updated pyproject.toml with correct package name and readme path, README.md under 100 lines, verified build with skill files included.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-status-handoff-distribution/05-01-SUMMARY.md
@.planning/phases/05-status-handoff-distribution/05-02-SUMMARY.md
@.planning/phases/05-status-handoff-distribution/05-03-SUMMARY.md

@Diligent/pyproject.toml
@Diligent/LICENSE
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve PyPI package name</name>
  <decision>Which package name to use on PyPI</decision>
  <context>
    The name "diligent" is taken on PyPI (v0.0.5, Jan 2023, abandoned parquet-database tool). The project needs an available name. Per CONTEXT.md, the fallback preference order is:
    1. Different real word from PRD list: tether, plumb, searchlight, bedrock, veritas, anchor
    2. Suffix: diligent-dd, diligent-cli
    3. Prefix: dd-diligent

    Research recommended checking "diligent-dd" first (short, memorable, CLI entry point still `diligent`).

    Before presenting options, the executor should check PyPI availability for the top candidates:
    - `pip index versions diligent-dd 2>&1` (or `curl -s https://pypi.org/pypi/diligent-dd/json`)
    - `pip index versions diligent-cli 2>&1`
    - Check a few PRD word options too

    The CLI entry point (`diligent`) stays the same regardless of package name.
  </context>
  <options>
    <option id="diligent-dd">
      <name>diligent-dd</name>
      <pros>Short, memorable, "dd" = due diligence mnemonic, pip install diligent-dd is clear</pros>
      <cons>Hyphenated name (minor; very common on PyPI)</cons>
    </option>
    <option id="diligent-cli">
      <name>diligent-cli</name>
      <pros>Clear it's a CLI tool, common naming pattern on PyPI</pros>
      <cons>Generic suffix, less memorable than diligent-dd</cons>
    </option>
    <option id="prd-word">
      <name>PRD alternate word (tether, plumb, bedrock, veritas, anchor)</name>
      <pros>Clean single-word name, no suffix needed</pros>
      <cons>Changes brand identity, may also be taken on PyPI</cons>
    </option>
  </options>
  <resume-signal>Select: diligent-dd, diligent-cli, prd-word (specify which), or provide another name</resume-signal>
</task>

<task type="auto">
  <name>Task 2: PyPI packaging, README, and build verification</name>
  <files>
    Diligent/pyproject.toml,
    Diligent/README.md
  </files>
  <action>
    **Update `Diligent/pyproject.toml`:**
    1. Change `name` to the resolved package name (from Task 1 decision)
    2. Add `readme = "README.md"` to [project] section
    3. Add classifiers for PyPI:
       ```toml
       classifiers = [
           "Development Status :: 4 - Beta",
           "Environment :: Console",
           "Intended Audience :: Financial and Insurance Industry",
           "Topic :: Office/Business",
           "Programming Language :: Python :: 3",
           "Programming Language :: Python :: 3.11",
           "Programming Language :: Python :: 3.12",
           "Programming Language :: Python :: 3.13",
       ]
       ```
    4. Add `[project.urls]` section:
       ```toml
       [project.urls]
       Repository = "https://github.com/bmasterson1/diligent"
       ```
    5. Ensure hatchling build config includes the skills directory. The default hatchling behavior includes all git-tracked files under the package directory. If needed, add:
       ```toml
       [tool.hatch.build.targets.wheel]
       packages = ["diligent"]
       ```
    6. Verify the `[project.scripts]` entry point stays as `diligent = "diligent.cli:cli"` regardless of package name.

    **Create `Diligent/README.md`:**
    Under 100 lines. Structure per CONTEXT.md:

    1. **Title + one-paragraph description** (~5 lines): What diligent is (CLI for due diligence state management), who it's for (M&A analysts, deal teams), what it does (tracks facts, sources, artifacts, questions across a deal lifecycle).

    2. **Install** (~3 lines):
       ```
       pipx install {package-name}
       ```
       Or `pip install {package-name}`. Mention Python 3.11+ requirement.

    3. **Quickstart** (~30-40 lines): 5-7 numbered steps covering the core loop:
       1. `diligent init` -- scaffold a deal folder
       2. `diligent ingest <path>` -- register a source document
       3. `diligent truth set <key> <value> --source <id>` -- record a fact
       4. `diligent artifact register <path> --references <keys>` -- register a deliverable
       5. `diligent reconcile` -- check what's stale
       6. `diligent status` -- see full deal state
       7. `diligent handoff --clip` -- restore AI context in a new session
       Each step: one command + one sentence explanation.

    4. **AI agent setup** (~5 lines): `diligent install --claude-code` or `--antigravity` to install skill files.

    5. **What it is not** (~10 lines): Not a VDR, not a project management tool, not a replacement for Word/Excel, not a database, not a web app. One line each.

    6. **License** (~3 lines): BSL 1.1, free for individual use, commercial service-provider use requires separate license.

    Total: aim for 70-90 lines.

    **Build verification:**
    Run `hatch build` and verify the wheel contents include:
    - diligent/skills/dd_truth.md
    - diligent/skills/dd_sources.md
    - diligent/skills/dd_artifacts.md
    - diligent/skills/dd_questions.md
    - diligent/skills/dd_workstreams.md
    - diligent/skills/dd_status.md
    - diligent/skills/__init__.py
    - All command modules
    - README.md referenced as long_description

    If skills are missing from wheel, add explicit hatchling include configuration.

    **Full test suite run:** `python -m pytest tests/ -x --timeout=30` to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd Diligent && hatch build && python -c "
import zipfile, sys
whl = list(__import__('pathlib').Path('dist').glob('*.whl'))[-1]
with zipfile.ZipFile(whl) as z:
    names = z.namelist()
    skills = [n for n in names if 'skills/dd_' in n]
    assert len(skills) == 6, f'Expected 6 skill files in wheel, got {len(skills)}: {skills}'
    print(f'Wheel contains {len(skills)} skill files')
    print(f'Total files in wheel: {len(names)}')
" && python -m pytest tests/ -x --timeout=30</automated>
  </verify>
  <done>
    - pyproject.toml has resolved package name, readme field, classifiers, urls
    - README.md is under 100 lines with description, install, quickstart, AI agent setup, "what it is not", license
    - hatch build succeeds and wheel contains 6 skill files
    - CLI entry point is 'diligent' in the built package
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- `cd Diligent && hatch build` succeeds
- `cd Diligent && python -m pytest tests/ -v` full suite passes
- `cd Diligent && python -m pytest tests/test_cli_startup.py -x` startup under 200ms
- `wc -l Diligent/README.md` shows under 100 lines
- Wheel inspection confirms skill files included
</verification>

<success_criteria>
- Package builds cleanly with hatch
- Built wheel includes all skill template files
- pyproject.toml has correct name, readme, classifiers
- README is concise (under 100 lines) and covers install, quickstart, "what it is not"
- CLI entry point is `diligent` regardless of package name
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-status-handoff-distribution/05-04-SUMMARY.md`
</output>

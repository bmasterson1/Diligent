---
phase: 03-artifacts-reconciliation
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - diligent/diligent/commands/artifact_cmd.py
  - diligent/diligent/cli.py
  - diligent/tests/test_artifact_cmd.py
autonomous: true
requirements: [ART-01, ART-03, ART-04]

must_haves:
  truths:
    - "Analyst can register a deliverable with explicit fact dependencies via artifact register"
    - "Re-registering an existing artifact shows current references and exits non-zero without --confirm"
    - "Analyst can list all registered artifacts with live staleness status"
    - "Analyst can mark an artifact as refreshed, updating its last_refreshed timestamp"
    - "All artifact commands support --json output"
  artifacts:
    - path: "diligent/diligent/commands/artifact_cmd.py"
      provides: "artifact register, artifact list, artifact refresh CLI commands"
      exports: ["artifact_cmd"]
    - path: "diligent/diligent/cli.py"
      provides: "LazyGroup registration for artifact command group"
      contains: "artifact"
  key_links:
    - from: "diligent/diligent/commands/artifact_cmd.py"
      to: "diligent/diligent/state/artifacts.py"
      via: "read_artifacts / write_artifacts"
      pattern: "from diligent\\.state\\.artifacts import"
    - from: "diligent/diligent/commands/artifact_cmd.py"
      to: "diligent/diligent/state/truth.py"
      via: "read_truth for key validation and staleness"
      pattern: "from diligent\\.state\\.truth import"
    - from: "diligent/diligent/commands/artifact_cmd.py"
      to: "diligent/diligent/state/sources.py"
      via: "read_sources for staleness detection in artifact list"
      pattern: "from diligent\\.state\\.sources import"
    - from: "diligent/diligent/cli.py"
      to: "diligent/diligent/commands/artifact_cmd.py"
      via: "LazyGroup lazy_subcommands entry"
      pattern: "artifact.*artifact_cmd"
---

<objective>
Artifact CLI commands: register (with --confirm upsert gate and truth key validation), list (with --stale filter and live staleness status), and refresh (timestamp update).

Purpose: These are the analyst-facing commands for managing deliverable tracking. Register links artifacts to truth keys, list shows current state, refresh marks an artifact as up-to-date. Together they complete the artifact management workflow that reconcile then reads.

Output: artifact_cmd.py with three subcommands, LazyGroup registration, full test coverage.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-artifacts-reconciliation/03-CONTEXT.md
@.planning/phases/03-artifacts-reconciliation/03-RESEARCH.md
@.planning/phases/03-artifacts-reconciliation/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From diligent/diligent/state/models.py (created in Plan 01):
```python
@dataclass
class ArtifactEntry:
    path: str          # Relative posix path from deal root (H2 heading)
    workstream: str
    registered: str    # ISO 8601 date
    last_refreshed: str  # ISO 8601 date
    references: list[str] = field(default_factory=list)  # Truth keys
    scanner_findings: list[str] = field(default_factory=list)
    notes: str = ""

@dataclass
class ArtifactsFile:
    artifacts: list[ArtifactEntry] = field(default_factory=list)
```

From diligent/diligent/state/artifacts.py (created in Plan 01):
```python
def read_artifacts(path: Path) -> ArtifactsFile: ...
def write_artifacts(path: Path, artifacts: ArtifactsFile) -> None: ...
```

From diligent/diligent/state/truth.py (existing):
```python
def read_truth(path: Path) -> TruthFile: ...
```

From diligent/diligent/state/sources.py (existing):
```python
def read_sources(path: Path) -> SourcesFile: ...
```

From diligent/diligent/helpers/formatting.py (existing):
```python
def output_result(data: Any, json_mode: bool) -> None: ...
```

From diligent/diligent/commands/truth_cmd.py (pattern to follow):
```python
def _find_diligence_dir(env_cwd: Optional[str] = None) -> Path: ...
# Uses DILIGENT_CWD env var for test isolation
```

CLI registration pattern (diligent/diligent/cli.py):
```python
lazy_subcommands={
    "init": "diligent.commands.init_cmd.init_cmd",
    "truth": "diligent.commands.truth_cmd.truth_cmd",
    # Add: "artifact": "diligent.commands.artifact_cmd.artifact_cmd",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: artifact register command with --confirm upsert gate</name>
  <files>
    diligent/diligent/commands/artifact_cmd.py,
    diligent/tests/test_artifact_cmd.py
  </files>
  <behavior>
    - Test 1: register creates new artifact entry in ARTIFACTS.md with path, workstream, references, today's date for registered and last_refreshed
    - Test 2: register normalizes path to posix forward slashes relative to deal root
    - Test 3: register with --references key1,key2,key3 stores comma-separated keys as list
    - Test 4: register warns on truth keys not found in TRUTH.md (but succeeds)
    - Test 5: re-registering existing artifact without --confirm shows current references and exits 1
    - Test 6: re-registering with --confirm updates references and last_refreshed (upsert)
    - Test 7: register with --workstream sets workstream field
    - Test 8: register with --notes sets notes field
    - Test 9: register --json outputs structured result
    - Test 10: register fails with exit 1 if .diligence/ not found
  </behavior>
  <action>
    Create diligent/diligent/commands/artifact_cmd.py with an `artifact_cmd` Click group. Implement `register` subcommand:

    - Arguments: `path` (required, the deliverable file path)
    - Options: `--references` (required, comma-separated truth keys), `--workstream` (optional, default ""), `--notes` (optional, default ""), `--confirm` (flag, for upsert), `--json` (flag)
    - Use `_find_diligence_dir` pattern (copy from truth_cmd.py, same DILIGENT_CWD env var approach)
    - Normalize the input path: resolve to absolute, compute relative to deal root (parent of .diligence/), convert to posix with `as_posix()`
    - Parse `--references` by splitting on comma and stripping whitespace
    - Validate references against TRUTH.md: read_truth, check each key exists, print WARNING for missing keys (do not fail)
    - Check for existing artifact with same normalized path:
      - If exists and not --confirm: print current references, print "Re-run with --confirm to update.", exit 1
      - If exists and --confirm: update references, last_refreshed to today, workstream, notes (upsert)
    - If new: create ArtifactEntry with today's date for registered and last_refreshed
    - Write via write_artifacts
    - Output result (text or --json)

    Use `os.environ.get("DILIGENT_CWD")` for test isolation. Follow exit-code gate pattern from truth_cmd.py truth_set.
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_artifact_cmd.py::TestArtifactRegister -x -q</automated>
  </verify>
  <done>artifact register creates entries in ARTIFACTS.md with normalized posix paths, validates truth keys with warnings, blocks re-registration without --confirm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: artifact list with --stale filter, artifact refresh, and CLI registration</name>
  <files>
    diligent/diligent/commands/artifact_cmd.py,
    diligent/diligent/cli.py,
    diligent/tests/test_artifact_cmd.py
  </files>
  <behavior>
    - Test 1: list shows all artifacts with status column (CURRENT, STALE, ADVISORY)
    - Test 2: list --stale filters to only stale and advisory artifacts
    - Test 3: list with no artifacts shows "No artifacts registered."
    - Test 4: list --json outputs structured list
    - Test 5: list detects value-changed staleness (fact date after artifact last_refreshed)
    - Test 6: list detects source-superseded staleness (source superseded after artifact last_refreshed)
    - Test 7: list shows flagged facts as ADVISORY (not STALE)
    - Test 8: list summary line shows counts
    - Test 9: refresh updates last_refreshed to today for existing artifact
    - Test 10: refresh exits 1 for unknown artifact path
    - Test 11: refresh --json outputs structured result
    - Test 12: CLI help shows artifact command group with register, list, refresh subcommands
  </behavior>
  <action>
    Add `list_cmd` subcommand to artifact_cmd.py:
    - Options: `--stale` (flag, show only stale/advisory), `--workstream` (optional filter), `--json` (flag)
    - Read ARTIFACTS.md, TRUTH.md, SOURCES.md
    - For each artifact, compute live staleness status:
      - Build superseded_by index from SOURCES.md (dict mapping old_source_id -> new SourceEntry that superseded it)
      - For each referenced truth key:
        - Value changed: `fact.date > artifact.last_refreshed` (compare ISO date strings, lexicographic works for ISO 8601)
        - Source superseded: fact's source is in superseded_by index AND the superseding source's date_received > artifact.last_refreshed
        - Flagged: fact.flagged is not None
      - Artifact status: STALE if any value_changed or source_superseded, ADVISORY if only flagged facts, CURRENT otherwise
    - Display aligned columns: path (40 chars), workstream (15 chars), status (10 chars)
    - If --stale: filter to STALE and ADVISORY only
    - If --workstream: filter to matching workstream
    - Summary line: "X stale, Y advisory, Z current"

    Add `refresh` subcommand:
    - Argument: `path` (required)
    - Option: `--json` (flag)
    - Normalize path same as register (posix, relative to deal root)
    - Find artifact by normalized path, exit 1 if not found
    - Update last_refreshed to today's date
    - Write via write_artifacts

    Update diligent/diligent/cli.py: add "artifact": "diligent.commands.artifact_cmd.artifact_cmd" to lazy_subcommands dict.
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_artifact_cmd.py -x -q</automated>
  </verify>
  <done>artifact list shows all artifacts with live staleness status (CURRENT/STALE/ADVISORY), --stale filter works, summary line shows counts. artifact refresh updates last_refreshed timestamp. artifact command group registered in CLI LazyGroup.</done>
</task>

</tasks>

<verification>
- `cd diligent && python -m pytest tests/test_artifact_cmd.py -v` -- all artifact command tests pass
- `cd diligent && python -m pytest tests/ -x -q` -- full suite passes
- `cd diligent && python -m diligent artifact --help` shows register, list, refresh subcommands
</verification>

<success_criteria>
- artifact register creates entries with normalized posix paths, --references (comma-sep), --confirm upsert, truth key validation warnings
- artifact list displays all artifacts with live staleness computation (value changed, source superseded, flagged), --stale filter, --workstream filter, summary line
- artifact refresh updates last_refreshed for existing artifact, exits 1 for unknown path
- All three commands support --json output
- artifact registered in CLI LazyGroup
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-artifacts-reconciliation/03-02-SUMMARY.md`
</output>

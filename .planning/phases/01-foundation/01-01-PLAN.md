---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Diligent/pyproject.toml
  - Diligent/LICENSE
  - Diligent/diligent/__init__.py
  - Diligent/diligent/cli.py
  - Diligent/diligent/state/__init__.py
  - Diligent/diligent/state/models.py
  - Diligent/diligent/state/config.py
  - Diligent/diligent/helpers/__init__.py
  - Diligent/diligent/helpers/io.py
  - Diligent/diligent/helpers/formatting.py
  - Diligent/diligent/commands/__init__.py
  - Diligent/diligent/commands/migrate_cmd.py
  - Diligent/tests/conftest.py
  - Diligent/tests/test_atomic_write.py
  - Diligent/tests/test_models.py
autonomous: true
requirements:
  - INIT-05
  - INIT-06
  - INIT-07
  - INIT-08
  - XC-03
  - XC-05
  - XC-08

must_haves:
  truths:
    - "Package installs in editable mode and `diligent --help` responds in under 200ms"
    - "Atomic writes survive simulated crash (temp file cleaned up, no partial state)"
    - "Atomic writes retry on PermissionError with exponential backoff"
    - "Validation failure in atomic_write preserves prior file state"
    - "BSL 1.1 license file exists at package root"
    - "No network imports exist anywhere in the package"
    - "`diligent migrate` prints 'No migrations needed for schema version 1' and exits 0"
  artifacts:
    - path: "Diligent/pyproject.toml"
      provides: "Package metadata, build config, console_scripts entry point"
      contains: "diligent = \"diligent.cli:cli\""
    - path: "Diligent/LICENSE"
      provides: "BSL 1.1 license text"
      contains: "Business Source License"
    - path: "Diligent/diligent/cli.py"
      provides: "LazyGroup CLI entry point"
      exports: ["cli"]
    - path: "Diligent/diligent/state/models.py"
      provides: "Dataclass definitions for all 6 state file types"
      exports: ["DealFile", "TruthFile", "FactEntry", "SupersededValue", "SourcesFile", "SourceEntry", "WorkstreamsFile", "WorkstreamEntry", "StateFile", "ConfigFile"]
    - path: "Diligent/diligent/helpers/io.py"
      provides: "Atomic write with OneDrive retry and validation"
      exports: ["atomic_write"]
    - path: "Diligent/diligent/helpers/formatting.py"
      provides: "Dual output (plain text + JSON) helpers"
      exports: ["output_result"]
    - path: "Diligent/diligent/commands/migrate_cmd.py"
      provides: "Stub migrate command for future schema migrations"
      exports: ["migrate"]
  key_links:
    - from: "Diligent/diligent/cli.py"
      to: "diligent.commands.*"
      via: "LazyGroup lazy_subcommands dict"
      pattern: "lazy_subcommands.*diligent\\.commands"
    - from: "Diligent/diligent/helpers/io.py"
      to: "os.replace"
      via: "atomic write with retry loop"
      pattern: "os\\.replace.*PermissionError"
---

<objective>
Package scaffold, typed models, atomic write utility, migrate stub, and CLI entry point with LazyGroup.

Purpose: Establishes the foundation every other plan depends on: installable package, typed data models for all 6 state files, crash-safe file I/O with OneDrive retry, a sub-200ms CLI shell, and a migrate stub for future schema changes.

Output: Installable Python package with `diligent --help` working, all dataclass models defined, atomic_write utility tested, formatting helpers ready, migrate command stubbed.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package scaffold, LICENSE, models, migrate stub, and LazyGroup CLI</name>
  <files>
    Diligent/pyproject.toml
    Diligent/LICENSE
    Diligent/diligent/__init__.py
    Diligent/diligent/cli.py
    Diligent/diligent/state/__init__.py
    Diligent/diligent/state/models.py
    Diligent/diligent/state/config.py
    Diligent/diligent/commands/__init__.py
    Diligent/diligent/commands/migrate_cmd.py
    Diligent/diligent/helpers/__init__.py
    Diligent/diligent/helpers/formatting.py
    Diligent/tests/__init__.py
    Diligent/tests/conftest.py
    Diligent/tests/test_models.py
  </files>
  <behavior>
    - Test: DealFile dataclass constructs with all required fields (deal_code, target_legal_name, target_common_name, deal_stage, loi_date, principal, principal_role, seller, broker, thesis, workstreams)
    - Test: FactEntry stores value as str, source as str, date as str; SupersededValue stores prior value/source/date
    - Test: TruthFile.facts is a dict keyed by fact key with FactEntry values
    - Test: SourceEntry has id, path, date_received, parties, workstream_tags, supersedes fields
    - Test: WorkstreamEntry has name, status fields
    - Test: ConfigFile has schema_version (int), deal_code (str), anchor_tolerance_pct (float), recent_window_days (int), workstreams (list)
    - Test: StateFile has created, last_modified fields
    - Test: `diligent --help` exits 0 and contains "diligent" in output (via CliRunner)
    - Test: LazyGroup lists commands (init, doctor, config, migrate) without importing their modules
    - Test: `diligent migrate` via CliRunner exits 0 and output contains "No migrations needed"
  </behavior>
  <action>
    Create the full package scaffold inside `Diligent/`:

    1. **pyproject.toml**: hatchling build backend, BSL 1.1 license (`license = "LicenseRef-BSL-1.1"`), console_scripts `diligent = "diligent.cli:cli"`, dependencies: click>=8.1, pyyaml>=6.0, python-frontmatter>=1.1. Dev deps: pytest>=8.0, pytest-cov>=5.0. requires-python = ">=3.11". Add `[tool.pytest.ini_options]` with `testpaths = ["tests"]`.

    2. **LICENSE**: Full BSL 1.1 text with Additional Use Grant: "You may use the Licensed Work for individual, non-commercial due diligence analysis. Commercial service-provider use requires a separate license." Licensor: Bryce Masterson. Licensed Work: diligent. Change Date: Four years from each release. Change License: Apache 2.0.

    3. **diligent/__init__.py**: `__version__ = "0.1.0"` only.

    4. **diligent/cli.py**: LazyGroup implementation per research Pattern 1. Register four lazy subcommands: init -> diligent.commands.init_cmd.init_cmd, doctor -> diligent.commands.doctor.doctor, config -> diligent.commands.config_cmd.config_cmd, migrate -> diligent.commands.migrate_cmd.migrate. Add `@click.version_option()`. Docstring: "diligent: due diligence state management."

    5. **diligent/state/models.py**: Stdlib dataclasses for all 6 state file types:
       - `SupersededValue(value: str, source: str, date: str)`
       - `FactEntry(key: str, value: str, source: str, date: str, workstream: str, supersedes: list[SupersededValue], computed_by: Optional[str], notes: Optional[str], flagged: Optional[dict])`
       - `TruthFile(facts: dict[str, FactEntry])`
       - `SourceEntry(id: str, path: str, date_received: str, parties: list[str], workstream_tags: list[str], supersedes: Optional[str], notes: Optional[str])`
       - `SourcesFile(sources: list[SourceEntry])`
       - `WorkstreamEntry(name: str, status: str)`
       - `WorkstreamsFile(workstreams: list[WorkstreamEntry])`
       - `DealFile(deal_code: str, target_legal_name: str, target_common_name: str, deal_stage: str, loi_date: str, principal: str, principal_role: str, seller: str, broker: str, thesis: str, workstreams: list[str])`
       - `StateFile(created: str, last_modified: str)`
       - `ConfigFile(schema_version: int, deal_code: str, created: str, anchor_tolerance_pct: float, recent_window_days: int, workstreams: list[str])`
       All fields use stdlib types only. Default factory for lists.

    6. **diligent/state/config.py**: `read_config(path: Path) -> ConfigFile` and `write_config(path: Path, config: ConfigFile) -> None`. Uses json.load/json.dumps with indent=2. Write uses atomic_write from helpers/io.py.

    7. **diligent/helpers/formatting.py**: `output_result(data: Any, json_mode: bool) -> None` that calls click.echo with json.dumps if json_mode, else formats as plain text. Also `output_findings(findings: list[dict], json_mode: bool) -> None` for doctor-style output per research Pattern 5.

    8. **diligent/commands/__init__.py**: Empty.

    9. **diligent/commands/migrate_cmd.py**: Stub click command `migrate` that reads config.json schema_version (if .diligence/ exists), prints "No migrations needed for schema version {N}" and exits 0. If .diligence/ does not exist, prints "No deal found. Run `diligent init` first." and exits 1. This is a placeholder for INIT-07; future schema changes will add real migration logic here.

    10. **tests/conftest.py**: Shared fixtures:
       - `tmp_deal_dir(tmp_path)` -- creates a tmp_path and returns it
       - `diligence_dir(tmp_deal_dir)` -- creates `.diligence/` inside tmp_deal_dir, returns the path
       - `cli_runner()` -- returns click.testing.CliRunner(mix_stderr=False)

    11. **tests/test_models.py**: Tests per behavior block above, including migrate command test via CliRunner.

    After creating all files, install in editable mode: `cd Diligent && pip install -e ".[dev]"`. Then run `pytest tests/test_models.py -x`.
  </action>
  <verify>
    <automated>cd Diligent && pytest tests/test_models.py -x --tb=short</automated>
  </verify>
  <done>
    - All dataclass models construct correctly with required fields and defaults
    - `diligent --help` returns exit code 0 via CliRunner
    - LazyGroup lists init, doctor, config, migrate without importing command modules
    - `diligent migrate` prints "No migrations needed" and exits 0
    - Package installed in editable mode
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic write utility with OneDrive retry and validation</name>
  <files>
    Diligent/diligent/helpers/io.py
    Diligent/tests/test_atomic_write.py
  </files>
  <behavior>
    - Test: atomic_write creates file with correct content
    - Test: atomic_write overwrites existing file atomically (no partial content on read)
    - Test: atomic_write creates parent directories if missing
    - Test: atomic_write cleans up temp file on failure
    - Test: atomic_write retries on PermissionError up to MAX_RETRIES times with exponential backoff
    - Test: atomic_write raises PermissionError after MAX_RETRIES exhausted
    - Test: atomic_write with validate_fn=None writes without validation
    - Test: atomic_write with validate_fn returning True writes normally
    - Test: atomic_write with validate_fn returning False raises ValueError and preserves prior file content
    - Test: atomic_write with validate_fn returning False does not create file if none existed before
    - Test: no temp files left behind after successful or failed writes
  </behavior>
  <action>
    Implement `diligent/helpers/io.py` per research Pattern 2:

    1. **atomic_write(target: Path, content: str, validate_fn: Optional[Callable] = None) -> None**
       - If validate_fn provided and returns False, raise ValueError immediately (prior state preserved, XC-05).
       - Create parent dirs with mkdir(parents=True, exist_ok=True).
       - tempfile.mkstemp(dir=target.parent, suffix=".tmp") to stay on same filesystem.
       - Write content with UTF-8 encoding, flush, fsync.
       - os.replace(tmp_path, target) with retry loop: MAX_RETRIES=5, BASE_DELAY=0.1s, exponential backoff.
       - Catch PermissionError on os.replace only. Re-raise after max retries.
       - On any exception, clean up temp file (try/except OSError on os.unlink).
       - No network imports. No external dependencies. Stdlib only.

    2. **Tests in test_atomic_write.py**:
       - Use tmp_path fixture for isolation.
       - Mock os.replace to simulate PermissionError for retry testing (use unittest.mock.patch).
       - Mock time.sleep to avoid actual delays in tests.
       - For validation tests: pass lambda that returns True/False.
       - Verify no .tmp files remain in directory after each test (glob for *.tmp).

    Run tests: `pytest tests/test_atomic_write.py -x`.
  </action>
  <verify>
    <automated>cd Diligent && pytest tests/test_atomic_write.py -x --tb=short</automated>
  </verify>
  <done>
    - atomic_write creates, overwrites, and validates files correctly
    - PermissionError retry with exponential backoff works (mocked)
    - Validation failure preserves prior state (XC-05)
    - No temp file leaks in any code path
    - All tests green
  </done>
</task>

</tasks>

<verification>
```bash
cd Diligent && pytest tests/ -x --tb=short
diligent --help
diligent migrate
python -c "from diligent.state.models import TruthFile, DealFile, ConfigFile; print('Models OK')"
python -c "from diligent.helpers.io import atomic_write; print('IO OK')"
```
</verification>

<success_criteria>
- Package installs and `diligent --help` works in under 200ms
- All 6 state file models defined as dataclasses with correct fields
- Atomic write utility handles: normal write, retry on PermissionError, validation gate, temp file cleanup
- BSL 1.1 LICENSE file present
- `diligent migrate` stub works and is registered in LazyGroup
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

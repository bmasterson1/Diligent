---
phase: 04-workstreams-tasks-questions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - diligent/diligent/state/models.py
  - diligent/diligent/state/workstreams.py
  - diligent/diligent/state/questions.py
  - diligent/diligent/templates/workstreams/financial.md
  - diligent/diligent/templates/workstreams/retention.md
  - diligent/diligent/templates/workstreams/technical.md
  - diligent/diligent/templates/workstreams/legal.md
  - diligent/diligent/templates/workstreams/hr.md
  - diligent/diligent/templates/workstreams/integration.md
  - diligent/diligent/templates/ws_context.md.tmpl
  - diligent/diligent/templates/ws_research.md.tmpl
  - diligent/diligent/templates/task_summary.md.tmpl
  - diligent/diligent/templates/task_plan.md.tmpl
  - diligent/diligent/templates/task_verification.md.tmpl
  - diligent/diligent/templates/task_status.yaml.tmpl
autonomous: true
requirements:
  - WS-04
  - WS-06

must_haves:
  truths:
    - "WorkstreamEntry has description and created fields with backward-compatible defaults"
    - "QuestionEntry has answer, answer_source, and date_answered fields with None defaults"
    - "read_workstreams handles WORKSTREAMS.md files with and without the new fields"
    - "read_questions handles QUESTIONS.md files with and without answer fields"
    - "write_workstreams emits description and created only when populated"
    - "write_questions emits answer fields only when not None"
    - "6 tailored workstream CONTEXT.md templates exist with 3-4 commented section headers each"
    - "Generic RESEARCH.md template exists for all workstream types"
    - "Task directory templates exist for SUMMARY.md, PLAN.md, VERIFICATION.md, status.yaml"
  artifacts:
    - path: "diligent/diligent/state/models.py"
      provides: "Extended WorkstreamEntry and QuestionEntry dataclasses"
      contains: "description.*str"
    - path: "diligent/diligent/state/workstreams.py"
      provides: "Backward-compatible reader/writer for new WorkstreamEntry fields"
      exports: ["read_workstreams", "write_workstreams"]
    - path: "diligent/diligent/state/questions.py"
      provides: "Backward-compatible reader/writer for QuestionEntry answer fields"
      exports: ["read_questions", "write_questions"]
    - path: "diligent/diligent/templates/workstreams/financial.md"
      provides: "Tailored financial workstream CONTEXT.md template"
      min_lines: 10
    - path: "diligent/diligent/templates/ws_research.md.tmpl"
      provides: "Generic RESEARCH.md template for all workstreams"
      min_lines: 5
  key_links:
    - from: "diligent/diligent/state/workstreams.py"
      to: "diligent/diligent/state/models.py"
      via: "WorkstreamEntry import with new fields"
      pattern: "WorkstreamEntry"
    - from: "diligent/diligent/state/questions.py"
      to: "diligent/diligent/state/models.py"
      via: "QuestionEntry import with answer fields"
      pattern: "QuestionEntry"
---

<objective>
Extend the WorkstreamEntry and QuestionEntry models with new fields, update their readers/writers for backward compatibility, and create all template files needed by workstream and task commands.

Purpose: Every subsequent plan in Phase 4 depends on these model extensions and templates. Doing this first prevents merge conflicts and ensures backward compatibility with existing state files and tests.
Output: Updated models.py, workstreams.py, questions.py; 6 tailored workstream templates; generic templates for RESEARCH.md, task SUMMARY/PLAN/VERIFICATION, and task status.yaml.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workstreams-tasks-questions/04-CONTEXT.md
@.planning/phases/04-workstreams-tasks-questions/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From diligent/diligent/state/models.py (CURRENT - to be extended):
```python
@dataclass
class WorkstreamEntry:
    name: str
    status: str

@dataclass
class QuestionEntry:
    id: str
    question: str
    workstream: str
    owner: str  # self, principal, seller, broker, counsel
    status: str  # open, answered, deferred
    date_raised: str
    context: Optional[dict] = None
```

From diligent/diligent/state/workstreams.py (reader pattern):
```python
def read_workstreams(path: Path) -> WorkstreamsFile:
    # ...
    WorkstreamEntry(
        name=str(data.get("name", heading)),
        status=str(data.get("status", "active")),
    )

def write_workstreams(path: Path, ws: WorkstreamsFile) -> None:
    data = {"name": entry.name, "status": entry.status}
```

From diligent/diligent/state/questions.py (writer pattern):
```python
def _format_question_yaml(entry: QuestionEntry) -> str:
    lines.append(f'question: "{escaped_q}"')
    lines.append(f"workstream: {entry.workstream}")
    lines.append(f"owner: {entry.owner}")
    lines.append(f"status: {entry.status}")
    lines.append(f'date_raised: "{entry.date_raised}"')
    if entry.context is not None:
        # ... yaml.safe_dump context
```

From diligent/diligent/templates/__init__.py:
```python
TEMPLATE_DIR = Path(__file__).parent

def render_template(template_name: str, context: dict) -> str:
    template_path = TEMPLATE_DIR / template_name
    raw = template_path.read_text(encoding="utf-8")
    tmpl = Template(raw)
    return tmpl.safe_substitute(context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WorkstreamEntry and QuestionEntry models, update state layer readers/writers</name>
  <files>diligent/diligent/state/models.py, diligent/diligent/state/workstreams.py, diligent/diligent/state/questions.py, diligent/tests/test_models.py, diligent/tests/test_state_roundtrip.py, diligent/tests/test_questions_state.py</files>
  <behavior>
    - Test: WorkstreamEntry accepts description="" and created="" as defaults
    - Test: WorkstreamEntry(name="financial", status="active") still works (no required new args)
    - Test: QuestionEntry accepts answer=None, answer_source=None, date_answered=None as defaults
    - Test: QuestionEntry without answer fields still works (backward compat)
    - Test: read_workstreams on a WORKSTREAMS.md WITHOUT description/created fields returns entries with empty string defaults
    - Test: read_workstreams on a WORKSTREAMS.md WITH description/created fields populates them correctly
    - Test: write_workstreams omits description when empty string, omits created when empty string
    - Test: write_workstreams includes description and created when populated
    - Test: read_questions on a QUESTIONS.md WITHOUT answer fields returns entries with None defaults
    - Test: read_questions on a QUESTIONS.md WITH answer fields populates them correctly
    - Test: write_questions (_format_question_yaml) omits answer/answer_source/date_answered when None
    - Test: write_questions includes answer fields when populated
    - Test: Existing test suite still passes (backward compat guard)
  </behavior>
  <action>
1. Extend WorkstreamEntry in models.py:
   - Add `description: str = ""` field after status
   - Add `created: str = ""` field after description (ISO 8601)

2. Extend QuestionEntry in models.py:
   - Add `answer: Optional[str] = None` after context
   - Add `answer_source: Optional[str] = None` after answer
   - Add `date_answered: Optional[str] = None` after answer_source

3. Update read_workstreams in workstreams.py:
   - Add `description=str(data.get("description", ""))` to WorkstreamEntry construction
   - Add `created=str(data.get("created", ""))` to WorkstreamEntry construction

4. Update write_workstreams in workstreams.py:
   - After `data = {"name": ..., "status": ...}`, add:
     `if entry.description: data["description"] = entry.description`
     `if entry.created: data["created"] = entry.created`

5. Update read_questions in questions.py:
   - Add `answer=data.get("answer")` (returns None if absent)
   - Add `answer_source=data.get("answer_source")` (returns None if absent, must convert to str or None)
   - Add `date_answered=data.get("date_answered")` (returns None if absent)
   - Use pattern: `answer_source_raw = data.get("answer_source"); answer_source = str(answer_source_raw) if answer_source_raw is not None else None`

6. Update _format_question_yaml in questions.py:
   - After date_raised line, before context block, add:
     ```python
     if entry.answer is not None:
         escaped_a = entry.answer.replace("\\", "\\\\").replace('"', '\\"')
         lines.append(f'answer: "{escaped_a}"')
     if entry.answer_source is not None:
         lines.append(f"answer_source: {entry.answer_source}")
     if entry.date_answered is not None:
         lines.append(f'date_answered: "{entry.date_answered}"')
     ```

7. Run full existing test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_models.py tests/test_state_roundtrip.py tests/test_questions_state.py tests/test_verification_gate.py -x -q</automated>
  </verify>
  <done>WorkstreamEntry has description and created fields with backward-compatible defaults. QuestionEntry has answer, answer_source, and date_answered fields with None defaults. All existing tests pass. New fields round-trip correctly through read/write.</done>
</task>

<task type="auto">
  <name>Task 2: Create workstream and task template files</name>
  <files>diligent/diligent/templates/workstreams/financial.md, diligent/diligent/templates/workstreams/retention.md, diligent/diligent/templates/workstreams/technical.md, diligent/diligent/templates/workstreams/legal.md, diligent/diligent/templates/workstreams/hr.md, diligent/diligent/templates/workstreams/integration.md, diligent/diligent/templates/ws_context.md.tmpl, diligent/diligent/templates/ws_research.md.tmpl, diligent/diligent/templates/task_summary.md.tmpl, diligent/diligent/templates/task_plan.md.tmpl, diligent/diligent/templates/task_verification.md.tmpl, diligent/diligent/templates/task_status.yaml.tmpl</files>
  <action>
1. Create `diligent/diligent/templates/workstreams/` directory.

2. Create 6 tailored CONTEXT.md files (NOT .tmpl extension; these are static content files copied directly, not rendered via string.Template). Each file has:
   - H1 heading: `# ${WORKSTREAM_NAME} - Context`
   - A brief introductory HTML comment explaining purpose
   - 3-4 H2 sections with HTML comment guidance suggesting common areas to cover

   **financial.md:**
   - `## Revenue Quality` <!-- recurring vs one-time, concentration, churn -->
   - `## Cost Structure` <!-- fixed vs variable, COGS breakdown, margin trends -->
   - `## Working Capital` <!-- AR/AP days, inventory turns, cash conversion -->
   - `## Adjustments & Add-backs` <!-- owner comp, one-time expenses, normalization -->

   **retention.md:**
   - `## Customer Base` <!-- top 10 concentration, contract terms, renewal rates -->
   - `## Pricing & Terms` <!-- pricing model, escalators, competitive positioning -->
   - `## Churn Analysis` <!-- gross vs net, cohort analysis, reasons for loss -->

   **technical.md:**
   - `## Systems & Infrastructure` <!-- core platforms, age, tech debt -->
   - `## Team & Capabilities` <!-- org chart, key person risk, hiring pipeline -->
   - `## Product Roadmap` <!-- development pipeline, competitive moat, IP -->
   - `## Security & Compliance` <!-- certifications, audit history, data handling -->

   **legal.md:**
   - `## Corporate Structure` <!-- entity chart, jurisdictions, minority interests -->
   - `## Contracts & Agreements` <!-- material contracts, change of control, assignability -->
   - `## Litigation & Regulatory` <!-- pending claims, regulatory compliance, environmental -->

   **hr.md:**
   - `## Workforce Composition` <!-- headcount, tenure, turnover, key employees -->
   - `## Compensation & Benefits` <!-- salary benchmarks, benefit costs, equity plans -->
   - `## Employment Matters` <!-- handbook compliance, pending claims, retention risk -->

   **integration.md:**
   - `## Day One Readiness` <!-- critical integrations, communication plan, IT cutover -->
   - `## Synergy Targets` <!-- cost synergies, revenue synergies, timeline -->
   - `## Integration Risks` <!-- culture, customer disruption, system migration -->

3. Create `ws_context.md.tmpl` (generic fallback for custom workstream names):
   - H1: `# $WORKSTREAM_NAME - Context`
   - HTML comment: <!-- Define the scope, parties, and boundaries for this workstream -->
   - `## Scope` <!-- What this workstream investigates -->
   - `## Key Contacts` <!-- Who to talk to -->
   - `## Open Areas` <!-- What needs attention -->

4. Create `ws_research.md.tmpl` (generic for ALL workstreams, including the 6 templates):
   - H1: `# $WORKSTREAM_NAME - Research`
   - HTML comment: <!-- Growing record of findings. Add entries as you work. -->
   - Empty body (research is created during work, not scaffolded)

5. Create `task_summary.md.tmpl`:
   - H1: `# $TASK_DESC`
   - HTML comment: <!-- Write your findings, analysis, and conclusions here. This file must be non-empty to mark the task complete. -->

6. Create `task_plan.md.tmpl`:
   - H1: `# Plan: $TASK_DESC`
   - HTML comment: <!-- Optional. Outline your approach before starting work. -->

7. Create `task_verification.md.tmpl`:
   - H1: `# Verification: $TASK_DESC`
   - HTML comment: <!-- Optional. Record how you verified your work. -->

8. Create `task_status.yaml.tmpl` (rendered via string.Template):
   ```
   description: "$TASK_DESC"
   status: open
   created: "$ISO_DATE"
   ```

Note: The 6 tailored workstream templates in `workstreams/` are plain .md files, not .tmpl files. They contain `$WORKSTREAM_NAME` as a string.Template variable that gets substituted when copied. The `ws_context.md.tmpl` and `ws_research.md.tmpl` are .tmpl files used via render_template for custom workstream names.

Actually, for consistency: the 6 tailored templates should ALSO use $WORKSTREAM_NAME for the H1 heading so they can be rendered via render_template. Store them as .md files but read and substitute when copying. Or just hardcode the workstream name in the H1. Since the workstream name is known (financial, retention, etc.), hardcode it in the H1. Simpler.
  </action>
  <verify>
    <automated>cd diligent && python -c "from pathlib import Path; templates = Path('diligent/templates/workstreams'); assert templates.is_dir(); assert len(list(templates.glob('*.md'))) == 6; assert Path('diligent/templates/ws_context.md.tmpl').exists(); assert Path('diligent/templates/ws_research.md.tmpl').exists(); assert Path('diligent/templates/task_summary.md.tmpl').exists(); assert Path('diligent/templates/task_plan.md.tmpl').exists(); assert Path('diligent/templates/task_verification.md.tmpl').exists(); assert Path('diligent/templates/task_status.yaml.tmpl').exists(); print('All template files exist')"</automated>
  </verify>
  <done>6 tailored workstream CONTEXT.md templates exist with 3-4 commented section headers each. Generic ws_context.md.tmpl and ws_research.md.tmpl exist. Task directory templates (summary, plan, verification, status.yaml) exist. All template files are readable and contain expected structure.</done>
</task>

</tasks>

<verification>
1. `cd diligent && python -m pytest tests/ -x -q` -- full existing test suite passes (backward compat)
2. All 6 workstream template files exist under `diligent/diligent/templates/workstreams/`
3. Generic templates exist: ws_context.md.tmpl, ws_research.md.tmpl, task_summary.md.tmpl, task_plan.md.tmpl, task_verification.md.tmpl, task_status.yaml.tmpl
4. WorkstreamEntry and QuestionEntry model extensions have backward-compatible defaults
</verification>

<success_criteria>
- WorkstreamEntry has description (str, default "") and created (str, default "") fields
- QuestionEntry has answer (Optional[str]), answer_source (Optional[str]), date_answered (Optional[str]) fields
- State layer readers handle files with and without the new fields
- State layer writers conditionally emit new fields only when populated
- All 12 template files exist and contain appropriate content
- Full existing test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-workstreams-tasks-questions/04-01-SUMMARY.md`
</output>

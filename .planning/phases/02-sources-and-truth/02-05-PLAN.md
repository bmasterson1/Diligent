---
phase: 02-sources-and-truth
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - diligent/diligent/helpers/diff_excel.py
  - diligent/diligent/helpers/diff_docx.py
  - diligent/diligent/commands/sources_cmd.py
  - diligent/diligent/pyproject.toml
  - diligent/tests/test_diff_excel.py
  - diligent/tests/test_sources_diff.py
  - diligent/tests/fixtures/sample_a.xlsx
  - diligent/tests/fixtures/sample_b.xlsx
  - diligent/tests/fixtures/sample_a.docx
  - diligent/tests/fixtures/sample_b.docx
autonomous: true
requirements: [SRC-05, SRC-06, SRC-07]

must_haves:
  truths:
    - "diff_excel_summary compares two Excel files and returns structured sheet/cell/row/named-range differences"
    - "diff_docx_summary compares two Word documents and returns paragraph-level difference counts"
    - "diligent sources diff <id-a> <id-b> shows structured diff between two source files"
    - "diligent ingest with --supersedes on an Excel file automatically shows compact diff summary"
    - "openpyxl and python-docx are lazy-imported (never at module top level)"
  artifacts:
    - path: "diligent/diligent/helpers/diff_excel.py"
      provides: "diff_excel_summary function for Excel comparison"
      exports: ["diff_excel_summary"]
    - path: "diligent/diligent/helpers/diff_docx.py"
      provides: "diff_docx_summary function for Word comparison"
      exports: ["diff_docx_summary"]
    - path: "diligent/diligent/commands/sources_cmd.py"
      provides: "sources diff subcommand + ingest auto-diff integration"
      contains: "sources_diff"
    - path: "diligent/tests/fixtures/sample_a.xlsx"
      provides: "Test fixture: Excel file A for diff comparison"
    - path: "diligent/tests/fixtures/sample_b.xlsx"
      provides: "Test fixture: Excel file B with known differences from A"
  key_links:
    - from: "diligent/diligent/commands/sources_cmd.py"
      to: "diligent/diligent/helpers/diff_excel.py"
      via: "diff_excel_summary for Excel sources"
      pattern: "from diligent\\.helpers\\.diff_excel import"
    - from: "diligent/diligent/commands/sources_cmd.py"
      to: "diligent/diligent/helpers/diff_docx.py"
      via: "diff_docx_summary for Word sources"
      pattern: "from diligent\\.helpers\\.diff_docx import"
    - from: "diligent/diligent/helpers/diff_excel.py"
      to: "openpyxl"
      via: "lazy import inside function body"
      pattern: "from openpyxl import load_workbook"
    - from: "diligent/diligent/helpers/diff_docx.py"
      to: "python-docx"
      via: "lazy import inside function body"
      pattern: "from docx import Document"
---

<objective>
Build the document diff helpers and wire them into the source commands: `diligent sources diff` for comparing two source files, and auto-diff on ingest when --supersedes points to an Excel file.

Purpose: When an analyst ingests a new version of a spreadsheet, they need to see what changed at a glance. The diff helpers provide structured comparison for Excel (sheets, cells, rows, named ranges) and Word (paragraph-level), with unsupported formats returning a clear message.

Output: diff_excel.py, diff_docx.py helpers, sources diff subcommand, ingest auto-diff integration, test fixtures.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sources-and-truth/02-CONTEXT.md
@.planning/phases/02-sources-and-truth/02-RESEARCH.md
@.planning/phases/02-sources-and-truth/02-02-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 02 output. -->

From diligent/diligent/commands/sources_cmd.py (after Plan 02):
```python
@click.group("sources")
def sources_cmd():
    """Manage source documents."""

# Subcommands: ingest, list, show (already created in Plan 02)
# This plan adds: diff subcommand, and auto-diff wiring in ingest
```

From diligent/diligent/state/sources.py:
```python
def read_sources(path: Path) -> SourcesFile:
# SourceEntry has: id, path (relative), date_received, parties, workstream_tags, supersedes, notes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Excel and Word diff helpers + test fixtures</name>
  <files>
    diligent/diligent/helpers/diff_excel.py,
    diligent/diligent/helpers/diff_docx.py,
    diligent/diligent/pyproject.toml,
    diligent/tests/test_diff_excel.py,
    diligent/tests/fixtures/sample_a.xlsx,
    diligent/tests/fixtures/sample_b.xlsx,
    diligent/tests/fixtures/sample_a.docx,
    diligent/tests/fixtures/sample_b.docx
  </files>
  <behavior>
    - Test: diff_excel_summary returns dict with keys: sheets_changed, total_sheets, cells_differ, rows_added, rows_removed, named_ranges_added, named_ranges_removed, changed_sheet_names
    - Test: diff_excel_summary detects cell value differences between two Excel files
    - Test: diff_excel_summary detects added/removed sheets
    - Test: diff_excel_summary detects row count differences
    - Test: diff_excel_summary detects named range differences
    - Test: diff_excel_summary uses read_only=True, data_only=True (lazy, low memory)
    - Test: openpyxl is imported inside the function body, not at module level
    - Test: diff_docx_summary returns dict with keys: paragraphs_changed, paragraphs_added, paragraphs_removed, total_paragraphs_a, total_paragraphs_b
    - Test: diff_docx_summary detects paragraph-level text differences
    - Test: python-docx is imported inside the function body, not at module level
    - Test: test fixtures exist and are valid Excel/Word files with known differences
  </behavior>
  <action>
    1. Create test fixtures programmatically in a conftest.py or test setup:
       - sample_a.xlsx: 2 sheets ("Summary", "Detail"), 10 rows each, 3 named ranges. Create using openpyxl in test setup.
       - sample_b.xlsx: same 2 sheets plus 1 new ("Notes"), "Detail" has 2 changed cells and 1 added row, 1 new named range. Create using openpyxl in test setup.
       - sample_a.docx: 5 paragraphs of text. Create using python-docx in test setup.
       - sample_b.docx: 6 paragraphs, 2 changed from sample_a. Create using python-docx in test setup.
       - Use pytest fixtures to create these in tmp_path rather than static files, so no binary fixtures in git.

    2. Create helpers/diff_excel.py:
       - `diff_excel_summary(path_a: str, path_b: str) -> dict`: Lazy-import openpyxl. Load both workbooks with read_only=True, data_only=True. Compare sheet names (detect added/removed). For common sheets, compare cell values row by row using iter_rows(). Count cells that differ, rows added (if one sheet has more rows), rows removed. Compare defined_names for named range differences. Always close workbooks in finally block.
       - Return dict with: sheets_changed, total_sheets (count in file B), cells_differ, rows_added, rows_removed, named_ranges_added, named_ranges_removed, changed_sheet_names (list of sheet names that have differences).

    3. Create helpers/diff_docx.py:
       - `diff_docx_summary(path_a: str, path_b: str) -> dict`: Lazy-import python-docx. Extract paragraph text from both documents. Use difflib.SequenceMatcher to compute differences. Count paragraphs changed (replaced), added, removed.
       - Return dict with: paragraphs_changed, paragraphs_added, paragraphs_removed, total_paragraphs_a, total_paragraphs_b.
       - `diff_docx_verbose(path_a: str, path_b: str) -> list[str]`: Same extraction but returns actual unified diff lines (for --verbose on sources diff). Uses difflib.unified_diff.

    4. Add openpyxl and python-docx to pyproject.toml dependencies:
       ```
       "openpyxl>=3.1.5",
       "python-docx>=1.1.0",
       ```
       These are runtime dependencies but lazy-imported. They are needed for diff functionality.

    5. Verify lazy import: in tests, assert that `sys.modules` does not contain 'openpyxl' or 'docx' before calling the diff functions. After calling, assert they are present. This confirms the lazy import pattern.
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_diff_excel.py -x -q</automated>
  </verify>
  <done>
    diff_excel_summary and diff_docx_summary produce correct structured comparison results. openpyxl and python-docx are lazy-imported. Test fixtures created programmatically. Dependencies added to pyproject.toml.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sources diff command + ingest auto-diff integration</name>
  <files>
    diligent/diligent/commands/sources_cmd.py,
    diligent/tests/test_sources_diff.py,
    diligent/tests/test_ingest.py
  </files>
  <behavior>
    - Test: sources diff <id-a> <id-b> with two Excel sources shows structured diff summary
    - Test: sources diff with two Word sources shows paragraph-level diff
    - Test: sources diff with unsupported format (PDF, txt) prints "Diff not supported for this format"
    - Test: sources diff with unknown source ID prints error and exits 1
    - Test: sources diff resolves source paths from SOURCES.md relative to deal root
    - Test: sources diff --json outputs structured diff result
    - Test: sources diff --verbose with Word sources shows actual paragraph diffs
    - Test: ingest with --supersedes pointing to an Excel source auto-prints compact diff summary
    - Test: ingest auto-diff compact format matches locked format from CONTEXT.md
    - Test: ingest with --supersedes pointing to non-Excel source skips auto-diff
    - Test: ingest auto-diff failure (missing file) prints warning but does not block ingest
  </behavior>
  <action>
    1. Add `sources_diff` subcommand to sources_cmd.py:
       - Arguments: ID_A (required), ID_B (required)
       - Options: --verbose (flag), --json (flag)
       - Flow: find .diligence/, read SOURCES.md, look up both source IDs. If either not found, error exit 1.
       - Resolve file paths: source.path is relative to deal root (parent of .diligence/). Build absolute paths.
       - Determine file type from extension:
         * .xlsx, .xls: call diff_excel_summary. Format as compact summary.
         * .docx: call diff_docx_summary. If --verbose, call diff_docx_verbose.
         * Everything else: print "Diff not supported for {extension} files."
       - Compact summary format for Excel (locked from CONTEXT.md):
         ```
         Diff: {id_a} vs {id_b}
           sheets changed: {n} of {total} ({sheet_names})
           cells differ:   {n}
           rows added:     {n}
           rows removed:   {n}
           named ranges:   +{n}, -{n}
         ```
       - Compact summary for Word:
         ```
         Diff: {id_a} vs {id_b}
           paragraphs changed:  {n}
           paragraphs added:    {n}
           paragraphs removed:  {n}
         ```
       - JSON: structured dict with diff_type ("excel"|"docx"|"unsupported") and all metrics.

    2. Update ingest command (in sources_cmd.py) to add auto-diff:
       - After successfully writing the new source entry to SOURCES.md, check if --supersedes was provided.
       - If --supersedes: look up the superseded source in the newly-written SOURCES.md (or the previously-read version). Resolve both file paths.
       - If both files are .xlsx or .xls: call diff_excel_summary. Print compact diff summary inline, followed by "Run `diligent sources diff {old_id} {new_id}` for full detail."
       - If files are not Excel: skip auto-diff silently (per CONTEXT.md: auto-diff only for Excel on ingest).
       - Wrap auto-diff in try/except: if file is missing or diff fails, print warning but do not fail the ingest itself. The source is already registered.

    3. Compact auto-diff format on ingest (locked from CONTEXT.md):
       ```
       Diff vs {old_id} ({old_filename}):
         sheets changed: {n} of {total} ({sheet_names})
         cells differ:   {n}
         rows added:     {n}
         rows removed:   {n}
         named ranges:   +{n}, -{n}

       Run `diligent sources diff {old_id} {new_id}` for full detail.
       ```
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_sources_diff.py tests/test_ingest.py -x -q</automated>
  </verify>
  <done>
    sources diff shows structured comparison for Excel and Word. Unsupported formats get clear message. Ingest auto-diffs Excel files on --supersedes. Auto-diff failure does not block ingest. All output formats match locked specifications.
  </done>
</task>

</tasks>

<verification>
cd diligent && python -m pytest tests/test_diff_excel.py tests/test_sources_diff.py tests/test_ingest.py -x -q
All diff and ingest auto-diff tests pass.
</verification>

<success_criteria>
- diff_excel_summary compares sheets, cells, rows, named ranges with openpyxl (read_only, data_only)
- diff_docx_summary compares paragraph-level text with python-docx
- Both helpers lazy-import their heavy dependencies
- sources diff resolves source paths from SOURCES.md, dispatches by file type
- Unsupported formats get "Diff not supported" message (no crash)
- Ingest auto-diffs Excel files when --supersedes is provided
- Auto-diff format matches CONTEXT.md locked specification exactly
- Auto-diff failure prints warning but does not block ingest
- Dependencies added to pyproject.toml
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-sources-and-truth/02-05-SUMMARY.md`
</output>

---
phase: 02-sources-and-truth
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - diligent/diligent/commands/truth_cmd.py
  - diligent/tests/test_truth_cmd.py
autonomous: true
requirements: [TRUTH-06, TRUTH-07, TRUTH-08]

must_haves:
  truths:
    - "truth list shows all facts with aligned columns and three-state status (current/flagged/stale)"
    - "truth list --stale shows flagged OR source-superseded facts"
    - "truth list --workstream scopes to one workstream"
    - "truth trace shows full revision history in reverse-chronological timeline"
    - "truth flag marks a fact for review with a reason"
  artifacts:
    - path: "diligent/diligent/commands/truth_cmd.py"
      provides: "truth list, trace, flag subcommands added"
      contains: "truth_list"
    - path: "diligent/tests/test_truth_cmd.py"
      provides: "Tests for list, trace, flag commands including staleness logic"
  key_links:
    - from: "diligent/diligent/commands/truth_cmd.py (truth_list)"
      to: "diligent/diligent/state/sources.py"
      via: "read_sources to build source supersedes reverse lookup for staleness"
      pattern: "read_sources"
    - from: "diligent/diligent/commands/truth_cmd.py (truth_trace)"
      to: "diligent/diligent/state/truth.py"
      via: "read_truth to access supersedes chain"
      pattern: "read_truth"
    - from: "diligent/diligent/commands/truth_cmd.py (truth_flag)"
      to: "diligent/diligent/state/truth.py"
      via: "read_truth / write_truth to set flagged dict"
      pattern: "write_truth"
---

<objective>
Build the truth display and management commands: `truth list` with staleness detection, `truth trace` for full revision history, and `truth flag` for marking facts for review.

Purpose: These commands let the analyst query and manage the fact registry. Staleness detection (cross-referencing SOURCES.md supersedes chains) is critical for `truth list --stale` and will later drive the reconcile engine in Phase 3.

Output: Three new subcommands added to truth_cmd.py: list, trace, flag.
</objective>

<execution_context>
@C:/Users/maste/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sources-and-truth/02-CONTEXT.md
@.planning/phases/02-sources-and-truth/02-RESEARCH.md
@.planning/phases/02-sources-and-truth/02-03-SUMMARY.md

<interfaces>
<!-- Interfaces from Plans 01-03 output. -->

From diligent/diligent/commands/truth_cmd.py (after Plan 03):
```python
@click.group("truth")
def truth_cmd():
    """Manage validated facts."""

# Subcommands: set, get (already created in Plan 03)
# This plan adds: list, trace, flag
```

From diligent/diligent/state/models.py:
```python
@dataclass
class FactEntry:
    key: str
    value: str
    source: str
    date: str
    workstream: str
    supersedes: list[SupersededValue] = field(default_factory=list)
    computed_by: Optional[str] = None
    notes: Optional[str] = None
    flagged: Optional[dict] = None  # {reason: str, date: str}
    anchor: bool = False

@dataclass
class SourceEntry:
    id: str
    path: str
    date_received: str
    parties: list[str] = field(default_factory=list)
    workstream_tags: list[str] = field(default_factory=list)
    supersedes: Optional[str] = None
    notes: Optional[str] = None
```

From diligent/diligent/state/truth.py:
```python
def read_truth(path: Path) -> TruthFile:
def write_truth(path: Path, truth: TruthFile) -> None:
```

From diligent/diligent/state/sources.py:
```python
def read_sources(path: Path) -> SourcesFile:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Truth list with staleness detection</name>
  <files>
    diligent/diligent/commands/truth_cmd.py,
    diligent/tests/test_truth_cmd.py
  </files>
  <behavior>
    - Test: truth list with no facts shows "No facts recorded" and summary
    - Test: truth list shows one line per fact: key, value (truncated to 30 chars), status, source ID
    - Test: fact with no flag and non-superseded source shows status "current"
    - Test: fact with flagged dict shows status "flagged"
    - Test: fact whose source has been superseded in SOURCES.md shows status "stale"
    - Test: --stale filter shows only flagged OR stale facts
    - Test: --workstream filter scopes to one workstream
    - Test: --stale and --workstream can combine
    - Test: summary line: "N facts: N current, N flagged, N stale"
    - Test: --json outputs structured JSON array
    - Test: aligned columns in plain text output
  </behavior>
  <action>
    1. Add `_build_superseded_source_set(sources: SourcesFile) -> set[str]` helper to truth_cmd.py: Build a set of source IDs that have been superseded by another source. Walk sources list, for each source with a `supersedes` field, add the superseded ID to the set. This set is used to determine if a fact's source has been replaced.

    2. Add `_compute_fact_status(fact: FactEntry, superseded_sources: set[str]) -> str` helper: Returns "flagged" if fact.flagged is not None. Returns "stale" if fact.source is in superseded_sources. Returns "current" otherwise.

    3. Add `truth_list` subcommand:
       - Options: --stale (flag), --workstream (string), --json (flag)
       - Flow: find .diligence/, read TRUTH.md, read SOURCES.md, build superseded set, compute status for each fact
       - Apply filters: --workstream filters by fact.workstream. --stale filters to status in ("flagged", "stale").
       - Plain text output: aligned columns. Column widths: key=25, value=30 (truncated with ...), status=8, source=15. Use f-string formatting.
       - Summary line: "N facts: N current, N flagged, N stale" (counts from filtered set? No -- summary always counts ALL facts, filters only affect which rows are displayed)
       - JSON: list of dicts with key, value, status, source, date, workstream, anchor

    4. Handle edge case: if SOURCES.md is empty or missing, superseded set is empty (all facts show as "current" from the staleness perspective).
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_truth_cmd.py::test_list -x -q 2>/dev/null; cd diligent && python -m pytest tests/test_truth_cmd.py -k "list" -x -q</automated>
  </verify>
  <done>
    truth list shows aligned per-fact output with three-state status. Staleness detects source supersedes chain. --stale and --workstream filters work. Summary line present. --json output works.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Truth trace and truth flag commands</name>
  <files>
    diligent/diligent/commands/truth_cmd.py,
    diligent/tests/test_truth_cmd.py
  </files>
  <behavior>
    - Test: truth trace <key> shows current value first, then supersedes chain in reverse chronological order
    - Test: truth trace format: "current  {value}  {source}  {date}  {source_path}" for top entry, then "         {value}  {source}  {date}  {source_path}" for prior entries
    - Test: truth trace includes flag events (raised, cleared) interleaved in timeline
    - Test: truth trace summary: "N values, N supersedes."
    - Test: truth trace with unknown key prints error and exits 1
    - Test: truth trace --json outputs structured timeline array
    - Test: truth flag <key> --reason "text" sets flagged dict {reason, date} on the fact
    - Test: truth flag updates TRUTH.md via write_truth
    - Test: truth flag on already-flagged fact updates the reason and date
    - Test: truth flag with unknown key prints error and exits 1
    - Test: truth flag --clear removes the flagged dict (sets to None)
    - Test: truth flag --json outputs structured result
  </behavior>
  <action>
    1. Add `truth_trace` subcommand:
       - Arguments: KEY (required)
       - Options: --json (flag)
       - Flow: find .diligence/, read TRUTH.md, look up key. If not found, error exit 1.
       - Build timeline: current value first, then supersedes chain entries (already reverse-chronological in the model).
       - For each entry in timeline, attempt to resolve source file path from SOURCES.md (read_sources, find entry by source ID, return path). If source not found in SOURCES.md, show source ID only.
       - Plain text format (locked from CONTEXT.md):
         ```
         current  $20,065   ARRIVAL-019  2026-04-07  Customer_List_v2.xlsx
                  $19,665   ARRIVAL-003  2026-03-15  Customer_List.xlsx
         ```
         Column alignment: "current" label (8 chars) on first row, blank on subsequent. Value (right-aligned or left, max 15 chars). Source ID (max 15). Date (10). Path (remainder, truncated).
       - If fact is flagged, include a flag event line: `flagged  {reason}  {date}` at the appropriate chronological position (after current value if flagged is set).
       - Summary line: "N values, N supersedes. Run with --verbose for diffs."
       - Note: --verbose is documented but implementation of inline diff summaries deferred to Plan 05 (which creates diff helpers). For now, --verbose prints same as default with a note "Verbose diff summaries require source files."
       - JSON: array of timeline entries, each with {label, value, source, date, path, type: "value"|"flag"}

    2. Add `truth_flag` subcommand:
       - Arguments: KEY (required)
       - Options: --reason (required string unless --clear), --clear (flag), --json (flag)
       - Flow: find .diligence/, read TRUTH.md, look up key. If not found, error exit 1.
       - If --clear: set fact.flagged = None. Print "Flag cleared for '{key}'."
       - If --reason: set fact.flagged = {"reason": reason, "date": today ISO}. Print "Flagged '{key}': {reason}".
       - Write TRUTH.md with write_truth.
       - JSON: {"status": "flagged"|"cleared", "key": key, "reason": reason}

    3. Ensure --reason and --clear are mutually exclusive (click.option with cls or manual check).
  </action>
  <verify>
    <automated>cd diligent && python -m pytest tests/test_truth_cmd.py -k "trace or flag" -x -q</automated>
  </verify>
  <done>
    truth trace shows full revision history in reverse-chronological timeline with source path resolution and flag events. truth flag marks/clears facts for review. Both support --json.
  </done>
</task>

</tasks>

<verification>
cd diligent && python -m pytest tests/test_truth_cmd.py -x -q
All truth command tests pass: set, get, list, trace, flag.
</verification>

<success_criteria>
- truth list shows aligned one-line-per-fact output with key, value, status, source
- Three status states work: current, flagged, stale (stale = source superseded in SOURCES.md)
- --stale filter shows flagged OR stale; --workstream scopes to one workstream
- Summary line always counts all facts
- truth trace shows reverse-chronological timeline with source path resolution
- Flag events interleaved in trace timeline
- truth flag sets/clears flagged dict with reason and date
- All commands support --json
</success_criteria>

<output>
After completion, create `.planning/phases/02-sources-and-truth/02-04-SUMMARY.md`
</output>
